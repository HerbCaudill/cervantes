/** Event emitted after in-app navigation updates the browser history. */
export const NAVIGATION_EVENT = "cervantes:navigate"

/** Event emitted before in-app navigation can replace the current reader layout. */
export const NAVIGATION_START_EVENT = "cervantes:navigation-start"
